module.exports = {
    plugins: {
        // include whatever plugin you want
        // but make sure you install these via yarn or npm !

        // add browser list config to package.json (see below)
        autoprefixer: ()
    }
}