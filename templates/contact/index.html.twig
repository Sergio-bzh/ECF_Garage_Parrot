{% extends 'base.html.twig' %}

{% block title %}Hello ContactController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/sergionunez/ecf_garage_parrot/src/Controller/ContactController.php'|file_link(0) }}">src/Controller/ContactController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/sergionunez/ecf_garage_parrot/templates/contact/index.html.twig'|file_link(0) }}">templates/contact/index.html.twig</a></code></li>
    </ul>
</div>
{#    <main>

        <div class="col col-10 mx-auto">
            {{ form_start(contactForm) }}
            {{ form_label(contactForm.user_name) }}
            {{ form_widget(contactForm.user_name) }}

            {{ form_label(contactForm.content) }}
            {{ form_widget(contactForm.content) }}

            {{ form_label(contactForm.score) }}
            {{ form_widget(contactForm.score) }}

            {{ form_rest(contactForm) }}
            <button class="btn btn-primary" type="submit">Enregistrer</button>
            {{ form_end(contactForm) }}
        </div>

#}    </main>
{% endblock %}
